# ── GRAFANA DATASOURCE ──
# This automatically connects Grafana to Prometheus
# No manual setup needed!

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090    # prometheus is the Docker service name
    isDefault: true
    editable: true
